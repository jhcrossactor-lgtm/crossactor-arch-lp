# crossactor-arch-lp — 3D VR 建築ビジュアライゼーション LP

CROSSACTOR の建築（3D VR パース制作）事業のランディングページ。

- ファーストビューは**スクロールで建物が立ち上がる**。図面の線が引かれ、構造材が組まれ、骨組みの写真が下から立ち上がり、完成パースが塗られ、最後は VR 表示になってマウスで見回せる。
- 事例セクションは、360度写真の**ビフォーアフター比較スライダー**。つまみで比べ、画像のドラッグで部屋を見回せる。

**コピーは公開中のポートフォリオサイト（jhcrossactor-lgtm.github.io/portfolio）をもとにした下書き**。
お問い合わせ先は仮リンク（`#contact`）。

## 動かす

```bash
python -m http.server 8124 -d .
```

`http://localhost:8124` を開く。ビルド不要・外部CDN無し（フォントも同梱）の素の HTML/CSS/JS。

## 構成

| ファイル | 中身 |
|---|---|
| `index.html` | SEQUENCE / USE CASES / BEFORE・AFTER / PROCESS / PROFILE / CTA |
| `styles.css` | 全スタイル。配色と書体は `:root` に集約 |
| `app.js` | 立面図と構造材の線の生成・スクロール演出・比較スライダー・ページ共通処理 |
| `tools/build_assets.py` | 素材の生成（Wikimedia からの取得と、自社素材の変換） |
| `assets/fonts/` | Space Mono / Inter（欧文サブセット）と OFL ライセンス文 |

## デザインの出どころ

`Crossactor/brand/brand-guide.md` は空だったため、**ロゴと公開中ポートフォリオサイトの CSS から**配色と書体を引いている。

| 用途 | 値 | 出どころ |
|---|---|---|
| 背景 | `#0a0a0f` | ポートフォリオ `--bg` |
| 線・強調 | `#00c8ff` | ロゴの回路線／ポートフォリオ `--cyan` |
| 構造材の線・タグ | `#7aba2a` | ロゴの家のアウトライン／`--green` |
| 見出し・ラベル | Space Mono | ポートフォリオ |
| 本文 | Inter ＋ 日本語はシステム書体 | ポートフォリオ |

ロゴは暗色背景で文字が沈むため、灰色部分だけを明るく置き換えた `assets/logo-light.png` を使っている（色の付いた線はそのまま）。

## 素材とライセンス

```bash
pip install pillow numpy
python tools/build_assets.py
```

| ファイル | 出どころ | ライセンス |
|---|---|---|
| `structure.webp` | Wikimedia Commons「Wood frame condominium construction in Canada」Marek Ślusarczyk | **CC BY 3.0**（フッターにクレジット必須） |
| `plan-lines.webp` | 自社の平面図 `heimenzu.jpg` をシアンの線画に変換 | 自社素材 |
| `room-before/cleared/styled.webp` | 自社の360度写真（現状 → AIで家具削除 → スタイリング） | 自社素材 |
| `render-living.webp` | 自社の完成パース | 自社素材 |
| `logo-light.png` | `新ロゴ.png` を暗色背景用に変換 | 自社素材 |

自社素材は `G:\google drive_jh\★　Crossactor\ポートフォリオ\` から読む。

採用しなかった素材：
- rawpixel の骨組み写真 … API 上は CC0 だが、配布ページが 403 でライセンスを確認できなかったため不採用
- stocksnap の写真 … CC0 は確認できたが、原寸が取得できず 960px しか使えないため不採用
- 人物入りの AI 生成画像 … 実在の事例と誤認されるため不採用

## スクロール演出（SEQUENCE）

`.seq` は `height:420vh`、中身の `.seq__stage` を sticky で固定し、通過中のスクロール量を 0→1 の進行度 `p` にしている。

| 進行度 | 起きること |
|---|---|
| 0 – 0.20 | 立面図の線が1本ずつ引かれる（図面を薄く敷く） |
| 0.22 – 0.42 | 構造材（柱・胴差し・小屋束・筋かい）の線が引かれる |
| 0.36 – 0.52 | 骨組みの写真が下から立ち上がる |
| 0.52 – 0.70 | 完成パースが左から塗られる |
| 0.76 – | VR 表示。フレーム上でマウスを動かすと見回せる |

尺を変えたいときは `.seq` の `height` を変える。

## 動作確認

Chromium（アプリ内ブラウザ）で確認した内容。

- 読み込み・外部通信：リソース 11件すべて成功、外部CDNへの通信なし、同梱フォント 3書体とも読み込み済み
- SEQUENCE：スクロール前に図面の線が自動で描かれる／進行度 0.32・0.47・0.6・0.85・0.92 で、構造線 → 骨組み写真の立ち上がり → 完成パースの塗り → VR 表示の順に切り替わることを数値と画面で確認
- BEFORE / AFTER：つまみで 25% に移動（`aria-valuenow` も追従）、画像ドラッグで 200px 回転、タブ切り替えで画像とラベルが入れ替わる
- レイアウト：デスクトップ（1134px）とスマホ幅で横スクロールなし、ヒーローの要素どうしの重なりなし
- 素材スクリプト：`tools/build_assets.py` の再実行で全素材が再生成される

アプリ内ブラウザはペインが描画されていないとスクロール連動の更新が止まるため、スクロール直後のスクリーンショットは1段階遅れる。
連続したアニメーションのなめらかさは、実ブラウザで一度スクロールして確認すること。

## 未確定事項

- **設計歴の年数**：ポートフォリオサイト内で「設計20年」と「建築設計25年」が混在しているため、LP には年数を入れていない
- **不動産事業**：ポートフォリオに不動産サービスの記載が無いため、LP には入れていない
- **お問い合わせ先**：未設定
